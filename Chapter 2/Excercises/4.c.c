#include <stdio.h>

int main(void) {
    int a, b, c;
    float x, y, z;

    printf("Integer values: %d, %d, %d\n", a, b, c);
    printf("Float values: %.2f, %.2f, %.2f\n", x, y, z);

    return 0;
}

